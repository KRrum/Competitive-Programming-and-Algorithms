Bunch of diffrent sorting algorithms, focused on those with time complexity O(nlogn) or O(n + M)
log_log is a task about sorting but with logn diffrent values, with allows to sort in O(nlog(logn))
K_chaotic is a sorting problem where each number is at most k indexes away from where it should be in the sorted array
